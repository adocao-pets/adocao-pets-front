import Menu from "@/components/Menu/menu"

export default function HomePage() {
  const mainStyle = {
    height: '100%',
  };
  return (
    <main style={mainStyle}>
      <Menu></Menu>
    </main>
  )
}
